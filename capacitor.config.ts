import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mealdb4.netlify.app',
  appName: 'mealdb-demo',
  webDir: 'dist/starting_app',
};

export default config;
